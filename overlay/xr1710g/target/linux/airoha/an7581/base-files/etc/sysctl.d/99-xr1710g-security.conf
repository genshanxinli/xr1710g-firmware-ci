# XR1710G security: hide kernel pointers from unprivileged users
# (SEC: kptr_restrict was previously set on-device only and lost on
# sysupgrade; firmware-side now. /proc/kallsyms addresses become 0 for
# non-root, mitigating pointer-leak based exploits.)
kernel.kptr_restrict=1
